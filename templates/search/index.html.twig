{{ form_start(form, { attr: {class: 'form-inline', action: path('searchResult')} }) }}
    {{ form_widget(form) }}
    <button type="submit" id="form_search" name="form[search]" class="btn btn-primary">{{ button_label|default('Rechercher') }}</button>
{{ form_end(form) }}